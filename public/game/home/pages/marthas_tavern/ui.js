export function setupUI() {
    // No initialization needed for UI components as they're defined in HTML
    // and event listeners are attached in characters.js
    
    // This function serves as a placeholder for future UI initialization needs
}